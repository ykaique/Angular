import { HighligthMouseDirective } from './highligth-mouse.directive';

describe('HighligthMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthMouseDirective();
    expect(directive).toBeTruthy();
  });
});
